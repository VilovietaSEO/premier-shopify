<section class="ip-section ip-section--surface ip-section--tight ip-section--flag ip-feature-strip full-width">
  <div class="ip-strip">
    <div class="ip-stack ip-strip__copy">
      {% if section.settings.eyebrow != blank %}
        <p class="ip-kicker">{{ section.settings.eyebrow }}</p>
      {% endif %}
      <h2 class="ip-heading ip-heading--medium">{{ section.settings.heading }}</h2>
    </div>
    <div class="ip-strip__items">
      {% for block in section.blocks %}
        {% liquid
          assign icon_name = block.settings.icon
          if icon_name == blank
            case forloop.index
              when 1
                assign icon_name = 'shield_check'
              when 2
                assign icon_name = 'microphone'
              else
                assign icon_name = 'clock'
            endcase
          endif
        %}
        <div class="ip-strip__item" {{ block.shopify_attributes }}>
          <span class="ip-strip__icon" aria-hidden="true">
            {% case icon_name %}
              {% when 'microphone' %}
                {{ 'ip-tool-icon-call-recording.svg' | inline_asset_content }}
              {% when 'clock' %}
                {{ 'ip-tool-icon-phone-times.svg' | inline_asset_content }}
              {% else %}
                {{ 'ip-tool-icon-auto-attendant.svg' | inline_asset_content }}
            {% endcase %}
          </span>
          <h3>{{ block.settings.title }}</h3>
          <p class="ip-note">{{ block.settings.body }}</p>
          {% if block.settings.included_label != blank %}
            <p class="ip-included-label">{{ block.settings.included_label }}</p>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "IP feature strip",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": ""
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Real phone service without smartphone drama."
    }
  ],
  "blocks": [
    {
      "type": "feature",
      "name": "Feature",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Calling"
        },
        {
          "type": "textarea",
          "id": "body",
          "label": "Body",
          "default": "Built around connection, not entertainment."
        },
        {
          "type": "text",
          "id": "included_label",
          "label": "Included label",
          "default": "Included in the Patriot Package"
        },
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "default": "shield_check",
          "options": [
            {
              "value": "shield_check",
              "label": "Shield check"
            },
            {
              "value": "microphone",
              "label": "Microphone"
            },
            {
              "value": "clock",
              "label": "Clock"
            }
          ]
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "IP feature strip",
      "blocks": [
        {
          "type": "feature",
          "settings": {
            "title": "Auto Attendant",
            "icon": "shield_check",
            "body": "Filter unwanted spam calls.",
            "included_label": "Included in the Patriot Package"
          }
        },
        {
          "type": "feature",
          "settings": {
            "title": "Call Recording",
            "icon": "microphone",
            "body": "Access to 90 days of recorded calls.",
            "included_label": "Included in the Patriot Package"
          }
        },
        {
          "type": "feature",
          "settings": {
            "title": "Quiet Hours",
            "icon": "clock",
            "body": "Set times for when to receive incoming calls.",
            "included_label": "Included in the Patriot Package"
          }
        },
        {
          "type": "feature",
          "settings": {
            "title": "Voicemail to Email",
            "icon": "microphone",
            "body": "Voicemail messages are sent to email as audio and transcription.",
            "included_label": "Included in the Patriot Package"
          }
        }
      ]
    }
  ]
}
{% endschema %}
