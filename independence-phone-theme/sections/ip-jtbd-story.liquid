<section id="{{ section.settings.anchor_id }}" class="ip-section ip-story">
  <div class="ip-story__intro">
    <div class="ip-stack">
      {% if section.settings.eyebrow != blank %}
        <p class="ip-kicker">{{ section.settings.eyebrow }}</p>
      {% endif %}
      <h2 class="ip-heading ip-heading--medium ip-heading--wide">{{ section.settings.heading }}</h2>
    </div>
    <div class="ip-stack">
      <div class="ip-lede">{{ section.settings.body }}</div>
      {% if section.settings.note != blank %}
        <p class="ip-note">{{ section.settings.note }}</p>
      {% endif %}
    </div>
  </div>

  {% if section.settings.solution_heading != blank or section.settings.solution_body != blank %}
    <div class="ip-story__solution">
      {% if section.settings.solution_heading != blank %}
        <h3>{{ section.settings.solution_heading }}</h3>
      {% endif %}
      {% if section.settings.solution_body != blank %}
        <div class="ip-lede">{{ section.settings.solution_body }}</div>
      {% endif %}
    </div>
  {% endif %}

  {% if section.blocks.size > 0 %}
    <div class="ip-story__cards">
      {% for block in section.blocks %}
        {% liquid
          case forloop.index
            when 1
              assign fallback_card_bg = 'ip-story-bus-days.webp' | asset_url
            when 2
              assign fallback_card_bg = 'ip-story-home-alone.webp' | asset_url
            when 3
              assign fallback_card_bg = 'ip-story-grandparents.webp' | asset_url
            else
              assign fallback_card_bg = 'ip-story-before-smartphone.webp' | asset_url
          endcase

          assign card_bg = fallback_card_bg
          if block.settings.image != blank
            assign card_bg = block.settings.image | image_url: width: 900
          endif
        %}
        <article class="ip-card ip-story-card" style="--ip-card-bg-image: url('{{ card_bg }}');" {{ block.shopify_attributes }}>
          <h3 class="ip-story-card__title">{{ block.settings.title }}</h3>
          <div class="ip-story-card__body">{{ block.settings.body }}</div>
        </article>
      {% endfor %}
    </div>
  {% endif %}

  {% if section.settings.cta_label != blank %}
    <div class="ip-story__cta">
      <a class="ip-button" href="{{ section.settings.cta_link | default: '/pages/order-now' }}">
        {{ section.settings.cta_label }}
      </a>
    </div>
  {% endif %}
</section>

{% schema %}
{
  "name": "IP JTBD story",
  "settings": [
    {
      "type": "text",
      "id": "anchor_id",
      "label": "Anchor ID",
      "default": "how-it-works"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Children need reachability. They do not need the whole internet yet."
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Body",
      "default": "<p>The \"First Phone\" decision should start with something practical: a bus route, an activity, a sleepover, or a few minutes home alone. The hard part is giving them a way to call without opening the door to apps, feeds, browsers, and constant negotiation. Independence Phone is built for parents who want communication before distraction. It gives children a simple way to call while helping parents delay the smartphone step until the child is ready.</p>"
    },
    {
      "type": "text",
      "id": "note",
      "label": "Small note"
    },
    {
      "type": "text",
      "id": "solution_heading",
      "label": "Solution heading"
    },
    {
      "type": "richtext",
      "id": "solution_body",
      "label": "Solution body"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "CTA label",
      "default": "Order now"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA link"
    }
  ],
  "blocks": [
    {
      "type": "moment",
      "name": "Family moment",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Background image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Bus days"
        },
        {
          "type": "richtext",
          "id": "body",
          "label": "Body",
          "default": "<p>A simple way to call home before and after school without needing a smartphone.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "IP JTBD story",
      "blocks": [
        {
          "type": "moment",
          "settings": {
            "title": "Bus days",
            "body": "<p>A simple way to call home before and after school without needing a smartphone.</p>"
          }
        },
        {
          "type": "moment",
          "settings": {
            "title": "Home-alone minutes",
            "body": "<p>More independence for short windows while parents are only one call away.</p>"
          }
        },
        {
          "type": "moment",
          "settings": {
            "title": "Grandparents",
            "body": "<p>A familiar phone experience that makes family calls feel easy again.</p>"
          }
        },
        {
          "type": "moment",
          "settings": {
            "title": "Before the smartphone",
            "body": "<p>A calmer first step until the child is ready for apps, browsers, and social feeds.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
